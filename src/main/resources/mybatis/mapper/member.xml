<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.artissue.model.MemberMapper">

    <select id="findUsername" resultType="mDTO" parameterType="String">

        select * from member where member_id = #{member_id}

    </select>

    <select id="findUserId" resultType="mDTO" parameterType="String">

        select * from member where member_name = #{member_id}

    </select>

    <select id="existsByMembername" resultType="boolean" parameterType="String">

        SELECT CASE WHEN COUNT(*) > 0 THEN TRUE ELSE FALSE END
        FROM member
        WHERE member_id = #{member_id}
    </select>

    <insert id="insertMember" parameterType="mDTO">

        insert into member (member_id, member_pwd, role, enabled,member_name,member_email,member_phone,
        member_birth,member_gender,member_join_date,member_nickname)
        values (#{member_id}, #{member_pwd}, #{role},1,#{member_name},#{member_email},#{member_phone},
        #{member_birth},#{member_gender},NOW(),#{member_nickname});

    </insert>

    <select id="sameMemberName" parameterType="String">
        select count(*) from member where member_id =#{member_id}

    </select>

    <select id="findMemberPwd" parameterType="map" resultType="mDTO">
        SELECT member_pwd, member_id
        FROM member
        WHERE member_id = #{member_id}
        AND member_name = #{member_name}
        AND member_email = #{member_email}
    </select>

    <select id="findMemberId" parameterType="map" resultType="mDTO">
        select member_id,member_name,member_email from member where member_name=#{member_name} and member_email=#{member_email}
    </select>

    <update id="updatePassword" parameterType="String">
        update member set member_pwd = #{encodedPwd} where member_id = #{member_id}
    </update>

    <select id="findbyId" resultType="mDTO" parameterType="String">

        select * from member where member_id = #{member_id}

    </select>

    <insert id="oAuth2insert" parameterType="mDTO">
        insert into member (member_id, member_pwd, role, enabled, member_name, member_email, member_phone, member_birth, member_gender, member_join_date, member_nickname)
        VALUES (#{member_id}, #{member_pwd}, #{role}, 1, #{member_name}, #{member_email}, #{member_phone}, #{member_birth}, #{member_gender}, NOW(), #{member_nickname})
    </insert>

    <update id="oAuth2update" parameterType="mDTO">
        UPDATE member
        SET
        member_pwd = #{member_pwd},
        member_name = #{member_name},
        member_email = #{member_email},
        member_gender = #{member_gender},
        member_birth = #{member_birth},
        member_phone = #{member_phone}
        WHERE
        member_id = #{member_email}
    </update>

    <select id="userReserveList" parameterType="int" resultType="reserveDTO">
        SELECT DISTINCT
        r.reservation_id,
        r.exhibition_key,
        e.exhibition_title,
        e.exhibition_thumnail,
        r.reservation_date,
        r.reservation_buyer_view
        FROM reservation r
        LEFT JOIN exhibition e ON r.exhibition_key = e.exhibition_key
        WHERE r.member_key = #{member_key}
        AND r.reservation_pay = 1
        ORDER BY r.reservation_date DESC
    </select>

    <select id="userReserveCont" parameterType="String" resultType="reserveDTO">
        SELECT
        r.*
        FROM reservation r
        LEFT JOIN exhibition e ON r.exhibition_key = e.exhibition_key
        WHERE r.reservation_id = #{reservation_id}
        AND r.reservation_pay = 1
        ORDER BY r.reservation_date DESC
    </select>

    <select id="userExhibition" parameterType="int" resultType="exhibitionDTO">
        select * from exhibition where exhibition_key = #{exhibition_key}
    </select>

    <select id="userZzimList" parameterType="int" resultType="zzimDTO">
        select * from zzim where member_key = #{member_key}
    </select>

    <update id="memberUpdate" parameterType="mDTO">

        update member set member_name = #{member_name},member_nickname=#{member_nickname},
        member_phone=#{member_phone},member_email=#{member_email},member_birth=#{member_birth},
        member_gender=#{member_gender} where member_key=#{member_key}

    </update>

    <update id="updatePwd" parameterType="map">

        update member set member_pwd = #{member_pwd} where member_id= #{member_id}
    </update>

</mapper>