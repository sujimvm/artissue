<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.artissue.model.NoticeMapper">
    <select id="noticeList" resultType="noticeDTO">
        select * from notice order by notice_date desc
    </select>

    <select id="noticeCont" resultType="noticeDTO">
        select * from notice where notice_key = #{notice_key}
    </select>

    <insert id="noticeInsert" parameterType="noticeDTO">
        insert into notice (notice_title, notice_cont, notice_hit, notice_date)
        values (#{notice_title}, #{notice_cont}, 0, NOW())
    </insert>

    <update id="noticeModify" parameterType="noticeDTO">
        update notice set notice_title = #{notice_title}, notice_cont = #{notice_cont} where notice_key = #{notice_key}
    </update>

    <delete id="noticeDelete" parameterType="int">
        delete from notice where notice_key = #{notice_key}
    </delete>

</mapper>